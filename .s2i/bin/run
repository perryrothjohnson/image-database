#!/bin/sh
# Installs dependencies needed for ResourceSpace

# if there is an error, abort this shell script
set -eo pipefail

# compile ImageMagick from source and install it
cd ImageMagick-7.0.7-0
./configure
make
make install

# run the original run script
exec /usr/libexec/s2i/run
